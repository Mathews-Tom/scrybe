{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "sessions",
  "description": "Reading and searching the sessions under the configured storage root. No path is accepted or returned: a session is named by an opaque identity the service layer resolves beneath the root.",
  "windows": [
    "main"
  ],
  "permissions": [
    "allow-list-sessions",
    "allow-search-sessions"
  ]
}
