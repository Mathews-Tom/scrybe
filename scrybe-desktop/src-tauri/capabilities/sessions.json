{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "sessions",
  "description": "Reading, searching, inspecting, recovering, and re-noting the sessions under the configured storage root, and abandoning a search already in flight. No path is accepted or returned: a session is named by an opaque identity the service layer resolves beneath the root, and a query is named by an identifier the frontend minted for it. Revealing a session opens the folder that identity resolves to, never a path the caller chose.",
  "windows": [
    "main"
  ],
  "permissions": [
    "allow-list-sessions",
    "allow-search-sessions",
    "allow-cancel-query",
    "allow-get-session",
    "allow-read-notes",
    "allow-read-transcript-page",
    "allow-repair-session",
    "allow-regenerate-notes",
    "allow-reveal-session",
    "allow-copy-notes",
    "allow-copy-transcript"
  ]
}
