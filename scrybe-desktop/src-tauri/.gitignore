# Emitted by tauri-build from `tauri.conf.json` and the capability files
# on every build; regenerating it is part of the build, not of review.
gen/

# Regenerated by `build.rs` from its command list on every build, the
# same as `gen/`. The reviewed surface is that list and the capability
# files, not their derivation.
permissions/autogenerated/
