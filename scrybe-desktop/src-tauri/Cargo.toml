# The desktop host is a workspace of its own. The root workspace's jobs
# build the library and CLI graph with `--no-default-features`; adding a
# WebView host to that graph would put `tauri` (and, on Linux, a system
# WebKitGTK) into every one of those jobs. The gate reaches this
# manifest explicitly with `--manifest-path` instead.
[workspace]
members = ["."]

[package]
name = "scrybe-desktop"
version = "0.0.0"
description = "Tauri host for the Scrybe desktop application"
edition = "2021"
rust-version = "1.95"
license = "Apache-2.0"
authors = ["Mathews Tom"]
repository = "https://github.com/Mathews-Tom/scrybe"
publish = false

[lib]
name = "scrybe_desktop"
crate-type = ["lib"]

[[bin]]
name = "scrybe-desktop"
path = "src/main.rs"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
scrybe-application = { package = "scrybe-meeting-application", path = "../../scrybe-application" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tauri = { version = "2", features = ["tray-icon"] }
tauri-plugin-single-instance = "2"
ts-rs = "12"

[lints.rust]
unsafe_code = "forbid"

[lints.clippy]
all = { level = "warn", priority = -1 }
pedantic = { level = "warn", priority = -1 }
nursery = { level = "warn", priority = -1 }
unwrap_used = "warn"
expect_used = "warn"
panic = "warn"

# `toml` is already resolved in this graph by `tauri-build`, so naming it
# here adds no crate to the audited surface. It is what lets
# `tests/capability_audit.rs` read a TOML capability file rather than
# skip one, which `tauri-utils` loads unconditionally.
[dev-dependencies]
tempfile = "3.27.0"
toml = "0.8"
